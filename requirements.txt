# ============================================================
# Innovation Republic – Python-Abhängigkeiten
# Installation: pip install -r requirements.txt
# ============================================================

# --- Streamlit Frontend ---
streamlit>=1.28.0

# --- Datenbank ---
psycopg2-binary>=2.9.9
pgvector>=0.2.4

# --- Sprachmodell (CPU-only für Streamlit Cloud, kein GPU nötig) ---
# CPU-Wheels reduzieren Installationsgröße von ~2GB auf ~250MB
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
sentence-transformers>=2.2.2
transformers>=4.30.0

# --- KI-gestützte Query-Normalisierung (Option C, benötigt ANTHROPIC_API_KEY) ---
anthropic>=0.40.0

# --- Datenabruf & Parsing ---
httpx>=0.25.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# --- Übersetzung (kostenlos, kein API-Key) ---
deep-translator>=1.11.4

# --- Datenverarbeitung ---
numpy>=1.24.0
pandas>=2.0.0

# --- Konfiguration & Utilities ---
python-dotenv>=1.0.0
tenacity>=8.2.0
loguru>=0.7.0
